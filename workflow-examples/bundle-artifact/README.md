### Bundle Artifact

Bundles the project into a zip file and attaches it to the artifacts. The zip file can be downloaded from the GitHub Actions page as an artifact.

- `plugin.yml` - bundles the plugin artifact.
