### Deploy using Docker

This deployment method will build a Docker image and push it to AWS ECR.

- `theme-project.yml` - deploys a theme project.
- `plugin-project.yml` - deploys a plugin project.
- `infinum-project-with-submodule-theme.yml` - deploys an Infinum project with a submodule theme.
