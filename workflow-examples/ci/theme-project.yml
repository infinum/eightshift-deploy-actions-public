name: Continuous Integration - Theme

on:
  workflow_dispatch:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  node:
    name: Run Node checks
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Lint assets
        uses: infinum/eightshift-deploy-actions-public/.github/actions/lint/node@feature/update
        with:
          PROJECT_PATH: wp-content/themes/<theme_name>

  composer:
    name: Composer checks
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: PHP Static code analysis
        uses: infinum/eightshift-deploy-actions-public/.github/actions/lint/composer@feature/update
        with:
          PROJECT_PATH: wp-content/themes/<theme_name>
          WORDPRESS_GH_ACTIONS: ${{ secrets.WORDPRESS_GH_ACTIONS }}
